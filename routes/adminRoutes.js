const express = require('express');;

const router = express.Router();


router.get('/add-product', (req, tes, next) => {});

router.post('/add-product', (req, res, next) => {});

module.exports = {
    router: router
};